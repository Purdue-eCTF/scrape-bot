services:
    scrape_bot:
        container_name: scrape_bot
        build:
            dockerfile: ./Dockerfile
            context: .
        # ports:
        #     - "8081:8081"
        volumes:
            - ./temp:/app/temp
        restart: always
        networks:
            - infra
networks:
    infra:
